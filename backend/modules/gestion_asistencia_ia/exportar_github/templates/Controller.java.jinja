package com.proyecto.controllers;

import com.proyecto.dtos.{{ clase.nombre }}RequestDTO;
import com.proyecto.dtos.{{ clase.nombre }}ResponseDTO;
import com.proyecto.services.{{ clase.nombre }}Service;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.Parameter;
import io.swagger.v3.oas.annotations.media.Content;
import io.swagger.v3.oas.annotations.media.ExampleObject;
import io.swagger.v3.oas.annotations.media.Schema;
import io.swagger.v3.oas.annotations.responses.ApiResponse;
import io.swagger.v3.oas.annotations.responses.ApiResponses;
import io.swagger.v3.oas.annotations.tags.Tag;
import jakarta.validation.Valid;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import java.util.List;
import java.util.UUID;

@CrossOrigin(originPatterns = "*", allowedHeaders = "*", allowCredentials = "true")
@RestController
@RequestMapping("/api/{{ clase.nombre | lower }}s")
@Tag(name = "{{ clase.nombre }}", description = "API RESTful para la gestión y operaciones de {{ clase.nombre }}")
public class {{ clase.nombre }}Controller {

    @Autowired
    private {{ clase.nombre }}Service service;

    @Operation(
        summary = "Obtener todos los registros",
        description = "Retorna la lista completa de registros de {{ clase.nombre }}."
    )
    @ApiResponses(value = {
        @ApiResponse(
            responseCode = "200",
            description = "Lista obtenida exitosamente",
            content = @Content(schema = @Schema(implementation = {{ clase.nombre }}ResponseDTO.class))
        )
    })
    @GetMapping
    public ResponseEntity<List<{{ clase.nombre }}ResponseDTO>> getAll() {
        return ResponseEntity.ok(service.findAll());
    }

    @Operation(
        summary = "Obtener por ID",
        description = "Obtiene los detalles completos de un {{ clase.nombre }} mediante su UUID de identificación."
    )
    @ApiResponses(value = {
        @ApiResponse(
            responseCode = "200",
            description = "Registro encontrado",
            content = @Content(schema = @Schema(implementation = {{ clase.nombre }}ResponseDTO.class))
        ),
        @ApiResponse(responseCode = "404", description = "Registro no encontrado por el UUID proporcionado")
    })
    @GetMapping("/{id}")
    public ResponseEntity<{{ clase.nombre }}ResponseDTO> getById(
        @Parameter(description = "UUID único de {{ clase.nombre }}", example = "3fa85f64-5717-4562-b3fc-2c963f66afa6", required = true)
        @PathVariable UUID id
    ) {
        return ResponseEntity.ok(service.findById(id));
    }

    @Operation(
        summary = "Crear nuevo registro",
        description = "Crea un nuevo {{ clase.nombre }} en el sistema enviando la estructura JSON indicada en el Request Body."
    )
    @ApiResponses(value = {
        @ApiResponse(
            responseCode = "201",
            description = "Registro creado exitosamente",
            content = @Content(schema = @Schema(implementation = {{ clase.nombre }}ResponseDTO.class))
        ),
        @ApiResponse(responseCode = "400", description = "JSON enviado es inválido o faltan campos obligatorios")
    })
    @PostMapping
    public ResponseEntity<{{ clase.nombre }}ResponseDTO> create(
        @io.swagger.v3.oas.annotations.parameters.RequestBody(
            description = "JSON con los datos del nuevo {{ clase.nombre }}",
            required = true,
            content = @Content(schema = @Schema(implementation = {{ clase.nombre }}RequestDTO.class))
        )
        @Valid @RequestBody {{ clase.nombre }}RequestDTO dto
    ) {
        return ResponseEntity.status(HttpStatus.CREATED).body(service.create(dto));
    }

    @Operation(
        summary = "Actualizar existente",
        description = "Actualiza de forma completa todos los campos de un {{ clase.nombre }} existente especificado por su UUID."
    )
    @ApiResponses(value = {
        @ApiResponse(
            responseCode = "200",
            description = "Registro actualizado exitosamente",
            content = @Content(schema = @Schema(implementation = {{ clase.nombre }}ResponseDTO.class))
        ),
        @ApiResponse(responseCode = "404", description = "Registro no encontrado")
    })
    @PutMapping("/{id}")
    public ResponseEntity<{{ clase.nombre }}ResponseDTO> update(
        @Parameter(description = "UUID único de {{ clase.nombre }}", example = "3fa85f64-5717-4562-b3fc-2c963f66afa6", required = true)
        @PathVariable UUID id,
        @io.swagger.v3.oas.annotations.parameters.RequestBody(
            description = "JSON con los campos actualizados de {{ clase.nombre }}",
            required = true,
            content = @Content(schema = @Schema(implementation = {{ clase.nombre }}RequestDTO.class))
        )
        @Valid @RequestBody {{ clase.nombre }}RequestDTO dto
    ) {
        return ResponseEntity.ok(service.update(id, dto));
    }

    @Operation(
        summary = "Actualización parcial (PATCH)",
        description = "Actualiza únicamente los campos provistos en el body JSON de {{ clase.nombre }} sin modificar el resto de atributos."
    )
    @ApiResponses(value = {
        @ApiResponse(
            responseCode = "200",
            description = "Actualización parcial realizada con éxito",
            content = @Content(schema = @Schema(implementation = {{ clase.nombre }}ResponseDTO.class))
        ),
        @ApiResponse(responseCode = "404", description = "Registro no encontrado")
    })
    @PatchMapping("/{id}")
    public ResponseEntity<{{ clase.nombre }}ResponseDTO> patch(
        @Parameter(description = "UUID de {{ clase.nombre }}", example = "3fa85f64-5717-4562-b3fc-2c963f66afa6", required = true)
        @PathVariable UUID id,
        @io.swagger.v3.oas.annotations.parameters.RequestBody(
            description = "JSON con únicamente los campos a modificar",
            required = true,
            content = @Content(schema = @Schema(implementation = {{ clase.nombre }}RequestDTO.class))
        )
        @RequestBody {{ clase.nombre }}RequestDTO dto
    ) {
        return ResponseEntity.ok(service.patch(id, dto));
    }

    @Operation(
        summary = "Eliminar por ID",
        description = "Elimina un {{ clase.nombre }} por su UUID."
    )
    @ApiResponses(value = {
        @ApiResponse(responseCode = "204", description = "Registro eliminado exitosamente"),
        @ApiResponse(responseCode = "404", description = "Registro no encontrado")
    })
    @DeleteMapping("/{id}")
    public ResponseEntity<Void> delete(
        @Parameter(description = "UUID de {{ clase.nombre }} a eliminar", example = "3fa85f64-5717-4562-b3fc-2c963f66afa6", required = true)
        @PathVariable UUID id
    ) {
        service.deleteById(id);
        return ResponseEntity.noContent().build();
    }

    {% for metodo in clase.metodos %}
    @Operation(
        summary = "Lógica de negocio: {{ metodo.nombre }}",
        description = "Operación de negocio personalizada '{{ metodo.nombre }}'.\n\n" +
                      "**Parámetros requeridos:**\n" +
                      "{% if metodo.java_decl %}- {{ metodo.java_decl }}\n{% else %}- Ninguno\n{% endif %}\n" +
                      "**Tipo de retorno:** `{{ metodo.retorno }}`"
    )
    @ApiResponses(value = {
        @ApiResponse(
            responseCode = "200",
            description = "Operación personalizada '{{ metodo.nombre }}' ejecutada con éxito"{% if metodo.return_json_example %},
            content = @Content(mediaType = "application/json", schema = @Schema(implementation = Object.class), examples = @ExampleObject(name = "Respuesta {{ metodo.nombre }}", value = "{{ metodo.return_json_example }}")){% endif %}
        ),
        @ApiResponse(responseCode = "400", description = "Parámetros de entrada inválidos para '{{ metodo.nombre }}'"),
        @ApiResponse(responseCode = "500", description = "Error interno ejecutando la lógica de negocio '{{ metodo.nombre }}'")
    })
    {% if metodo.json_body_example %}
    @io.swagger.v3.oas.annotations.parameters.RequestBody(
        description = "Estructura JSON del cuerpo de la petición (Body) para la operación '{{ metodo.nombre }}'",
        required = false,
        content = @Content(
            mediaType = "application/json",
            schema = @Schema(implementation = Object.class),
            examples = @ExampleObject(
                name = "Ejemplo {{ metodo.nombre }}",
                summary = "Ejemplo de parámetros JSON para {{ metodo.nombre }}",
                value = "{{ metodo.json_body_example }}"
            )
        )
    )
    {% endif %}
    {% if metodo.retorno == 'void' %}
    @PostMapping("/{{ metodo.nombre }}")
    public ResponseEntity<Void> {{ metodo.nombre }}({% if metodo.spring_params %}{{ metodo.spring_params }}{% endif %}) {
        service.{{ metodo.nombre }}({% if metodo.call_args %}{{ metodo.call_args }}{% endif %});
        return ResponseEntity.ok().build();
    }
    {% else %}
    @PostMapping("/{{ metodo.nombre }}")
    public ResponseEntity<{{ metodo.retorno }}> {{ metodo.nombre }}({% if metodo.spring_params %}{{ metodo.spring_params }}{% endif %}) {
        return ResponseEntity.ok(service.{{ metodo.nombre }}({% if metodo.call_args %}{{ metodo.call_args }}{% endif %}));
    }
    {% endif %}
    {% endfor %}
}




